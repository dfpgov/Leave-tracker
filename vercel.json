{
  "buildCommand": "vite build --outDir dist/public",
  "outputDirectory": "dist/public",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
